<UserControl x:Class="IssueManager.Controls.SavedIssuesUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IssueManager.Controls"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="500"
             Background="White">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/IssueManager;component/Resources/HandyControlResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="300"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0 0 0 10">
            <Button Content="Navigation" Width="80" Margin="4" Style="{StaticResource ButtonPrimary}"/>
            <Button Content="Smart views" Width="80" Margin="2"/>
            <Button Content="Clashes" Width="80" Margin="2"/>
            <Button Content="Lists" Width="80" Margin="2"/>
            <Button Content="Issues" Width="80" Margin="2"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Right"
                    Margin="0 0 0 10">
            <Button Content="Add" Width="80" Margin="2"/>
            <Button Content="Edit" Width="80" Margin="2"/>
            <Button Content="Delete" Width="80" Margin="2"/>
            <Button Content="sort" Width="80" Margin="2"/>
            <TextBox Width="80" Margin="2"/>
        </StackPanel>
        <ScrollViewer Grid.Row="2" Margin="2" BorderBrush="Gray" BorderThickness="2"/>
        <StackPanel Grid.Row="3" Margin="0 5">
            <TextBlock Text="Comment:" Margin="0 0 0 2"/>

            <Grid Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                         Height="120"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         Margin="0 0 5 0"/>

                <Border Grid.Column="1" BorderBrush="Gray" BorderThickness="1" Margin="0">
                    <Image Stretch="UniformToFill" />
                </Border>
            </Grid>
        </StackPanel>
        <DataGrid x:Name="CommentGrid"
                  Grid.Row="4"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  RowHeight="30"
                  Margin="2 0 2 10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Comment" Width="*"/>
                <DataGridTextColumn Header="Created By" Width="120"/>
                <DataGridTextColumn Header="Snapshot"  Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>
