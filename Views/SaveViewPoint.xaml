<hc:Window x:Class="IssueManager.Views.AddIssueView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IssueManager.Views"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:viewModels="clr-namespace:IssueManager.ViewModels"
        Title="Add Issue" Height="500" Width="700"
        ResizeMode="NoResize" WindowStartupLocation="CenterScreen">

    <hc:Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/IssueManager;component/Resources/HandyControlResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <Style TargetType="TextBlock" BasedOn="{StaticResource TextBlockDefaultBold}">
                <Setter Property="HorizontalAlignment" Value="Left"/>
            </Style>
        </ResourceDictionary>
    </hc:Window.Resources>



    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ================================================== -->

        <!-- ================================================== -->

        <Grid Grid.Row="0" Margin="0 0 0 8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Title:" VerticalAlignment="Center" Margin="0 0 8 0"/>
                    <TextBox Grid.Column="1" Margin="4 4 4 4" Height="25" Width="330" HorizontalAlignment="Left"/>

                </Grid>

                <Grid Grid.Row="1" Margin="0 0 0 4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="130"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="130"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Assigned to:" VerticalAlignment="Center" Margin="0 0 8 0"/>
                    <ComboBox Grid.Column="1" Margin="4 4 20 4" Height="25"/>
                    
                    <TextBlock Grid.Column="2" Text="Area:" VerticalAlignment="Center" Margin="0 0 8 0"/>
                    <ComboBox Grid.Column="3" Margin="4 4 40 4" Height="25"/>
                </Grid>
            
                <Grid Grid.Row="2" Margin="0 0 0 4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="130"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="130"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Priority:" VerticalAlignment="Center" Margin="0 0 8 0"/>
                    <ComboBox Grid.Column="1" Margin="4 4 20 4" Height="25"/>
                    
                    <TextBlock Grid.Column="2" Text="Deadline:" VerticalAlignment="Center" Margin="0 0 8 0"/>
                    <DatePicker Grid.Column="3" Margin="4 4 20 4" Height="25"/>
                </Grid>
                
            </Grid>


            <StackPanel Grid.Column="1" Margin="10 0 0 0">
                <TextBlock Text="Labels:" Margin="0 0 0 4" />
                <!-- Replace this with a ComboCheckBox of labels later -->
                <ListBox Height="auto">
                    <ListBoxItem Content="Urgent"/>
                    <ListBoxItem Content="MEP"/>
                    <ListBoxItem Content="Open"/>
                </ListBox>
            </StackPanel>
        </Grid>


        <!-- ================================================== -->

        <!-- ================================================== -->

        <StackPanel Grid.Row="2" Margin="0 0 0 8">
            <TextBlock Text="Description" Margin="4"/>
            <TextBox Height="100"/>
        </StackPanel>

        <!-- ================================================== -->

        <Grid Grid.Row="3" Margin="0 0 0 8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="400"/>
            </Grid.ColumnDefinitions>

            <!--Remove Comments section-->
            <StackPanel Grid.Column="0" Margin="0 0 10 0">
                <TextBlock Text="Comment:" Margin="0 0 0 4"/>
                <TextBox AcceptsReturn="True" Height="100" VerticalScrollBarVisibility="Auto"/>
            </StackPanel>

            <StackPanel Grid.Column="1">
                <Border BorderBrush="LightGray" BorderThickness="1" Margin="0 18 0 0" CornerRadius="5">
                    <Image Height="100" Stretch="UniformToFill" Margin="0 0 0 0" />
                </Border>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 2">
                    <Button Content="📷" Width="35" Margin="2" Style="{StaticResource ButtonPrimary.Small}"/> <!-- This Button to change SC-->
                    <Button Content="🔄" Width="35" Margin="2" Style="{StaticResource ButtonInfo.Small}"/> <!-- This Button to Refresh View-->
                    <Button Content="🖍️" Width="35" Margin="2" Style="{StaticResource ButtonSuccess.Small}"/> <!-- This Button to go to markup editor-->
                    <Button Content="❌" Width="35" Margin="2" Style="{StaticResource ButtonDanger.Small}"/> <!-- This Button to remove the image-->
                </StackPanel>
            </StackPanel>
        </Grid>

        <!-- ================================================== -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Content="Help" Width="70" Margin="4" HorizontalAlignment="Left" Style="{StaticResource ButtonInfo}"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 10 0 0">
                <Button Content="Save" Width="70" Margin="4" Style="{StaticResource ButtonPrimary}"/>
                <Button Content="Cancel" Width="70" Margin="4" Style="{StaticResource ButtonDanger}"/>
            </StackPanel>
        </Grid>

    </Grid>
</hc:Window>