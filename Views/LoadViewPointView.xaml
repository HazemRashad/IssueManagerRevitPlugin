<hc:Window x:Class="IssueManager.Views.LoadViewPointView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IssueManager.Views"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:viewModels="clr-namespace:IssueManager.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance viewModels:LoadViewPointViewModel}"
        ResizeMode="NoResize" Title="LoadViewPointView" 
        Height="700" Width="500">

    <hc:Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/IssueManager;component/Resources/HandyControlResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary> 
    </hc:Window.Resources>

        <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0 5 0 10">
            <TextBlock Text="Project: " Margin="4 0 0 2" Style="{StaticResource TextBlockSubTitleBold}" HorizontalAlignment="Left"/>
            <ComboBox Margin="2,2,4,2" DockPanel.Dock="Right" />
        </DockPanel>


        <ScrollViewer Grid.Row="1"/>

        <StackPanel Grid.Row="2" Margin="0 5">
            <TextBlock Text="Description:" Margin="4 0 0 2" Style="{StaticResource TextBlockSubTitleBold}" HorizontalAlignment="Left"/>
            <Grid Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                
                <Border Grid.Column="0" BorderBrush="Gray" 
                        BorderThickness="1" Margin="0, 0, 4, 0"
                        CornerRadius="5">
                    <TextBlock Height="120"/>
                </Border>
                
                <Border Grid.Column="1" BorderBrush="Gray" 
                        BorderThickness="1" Margin="0"
                        CornerRadius="5">
                    <Image Stretch="UniformToFill" />
                </Border>
            </Grid>
        </StackPanel>


        <!-- Data Grid to be styled after we add some content so that we can see styling effects. -->

        <DataGrid x:Name="CommentGrid"
              Grid.Row="3"
              AutoGenerateColumns="False"
              IsReadOnly="True"
              HeadersVisibility="Column"
              RowHeight="30"
              Margin="4 0 4 10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Comment" Width="*"/>
                <DataGridTextColumn Header="Created By" Width="120"/>
                <DataGridTextColumn Header="Created At"  Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</hc:Window>
