<Window
    x:Class="IssueManager.Views.SaveViewPointView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:viewModel="clr-namespace:IssueManager.ViewModels"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance viewModel:SaveViewPointViewModel}"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen"
    Title="SaveViewPoint"
    Height="350"
    Width="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Add Issue" FontSize="18" FontWeight="Bold" Margin="0,0,0,20" Grid.Row="0"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Task Name:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
            <TextBox Text="{Binding TaskName}" Grid.Row="0" Grid.Column="1" Margin="0,5"/>

            <TextBlock Text="Comment:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
            <TextBox Text="{Binding Comment}" Grid.Row="1" Grid.Column="1" Margin="0,5"/>

            <TextBlock Text="View Type:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
            <StackPanel Orientation="Horizontal" Grid.Row="2" Grid.Column="1" Margin="0,5">
                <RadioButton Content="Linked View" GroupName="Type" IsChecked="{Binding IsLinked}" Margin="0,0,20,0"/>
                <RadioButton Content="Dependent Task" GroupName="Type" IsChecked="{Binding IsDependent}"/>
            </StackPanel>

            <TextBlock Text="Select Master:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
            <ComboBox ItemsSource="{Binding MasterList}" SelectedItem="{Binding SelectedMaster}" Grid.Row="3" Grid.Column="1" Margin="0,5"/>

            <TextBlock Text="Assigned To:" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
            <TextBox Text="{Binding AssignedTo}" Grid.Row="4" Grid.Column="1" Margin="0,5"/>

            <TextBlock Text="Flagged:" Grid.Row="5" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
            <CheckBox IsChecked="{Binding IsFlagged}" Grid.Row="5" Grid.Column="1" Margin="0,5"/>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Save"
                    Command="{Binding SaveCommand}"
                    Width="100"
                    Height="30"
                    Background="#0078D7"
                    Foreground="White"
                    FontWeight="SemiBold"/>
        </StackPanel>
    </Grid>
</Window>